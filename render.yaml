services:
  - type: web
    name: nse-bse-stock-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn stock_api_server:app --bind 0.0.0.0:$PORT --timeout 7200 --workers 1
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
